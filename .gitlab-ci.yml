# This file is a template, and might need editing before it works on your project.
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Docker.gitlab-ci.yml

# Build a Docker image with CI/CD and push to the GitLab registry.
# Docker-in-Docker documentation: https://docs.gitlab.com/ee/ci/docker/using_docker_build.html
#
# This template uses one generic job with conditional builds
# for the default branch and all other (MR) branches.

image: debian:bullseye

stages:
  - prepare
  - deploy
  - verify

variables:
  GIT_SSL_NO_VERIFY: "1"

.default-ssh-setup: &ssh_setup
  - echo "📦 Setting up SSH connection..."
  - mkdir -p ~/.ssh
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
  - chmod 600 ~/.ssh/id_rsa
  - echo -e "Host *\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
  - ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts

before_script:
  - apt-get update && apt-get install -y openssh-client rsync curl

prepare:
  stage: prepare
  script:
    - *ssh_setup
    - echo "✅ SSH setup complete and host known keys added."

deploy:
  stage: deploy
  only:
    - master
  script:
    - *ssh_setup
    - echo "🔁 Syncing project to remote host..."
    - rsync -az --delete --exclude='.git' --exclude='logs/' ./ "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH"

    - echo "🚀 Restarting Airflow stack remotely..."
    - ssh "$DEPLOY_USER@$DEPLOY_HOST" "
        cd $DEPLOY_PATH &&
        docker-compose down &&
        docker-compose up -d --build
      "

verify:
  stage: verify
  script:
    - *ssh_setup
    - echo "🔍 Verifying Airflow webserver health..."
    - ssh "$DEPLOY_USER@$DEPLOY_HOST" "
        curl -sf http://localhost:8080/health || (echo '❌ Airflow webserver unhealthy!' && exit 1)
      "

